"""Notes."""

import numpy as np

w = np.random.randn(5)  # Gaussian

# ston tipo prepei na einai h p  grammi oxi i stiliself.

xtrain = np.random.randn(135, 5)  # paradeigma

p = 25

# xtrain = epaujimenos
xtrain([p, :].dot(w))

if u > 0:
    y = 1
else:
    y = 0

# magkia
y = (u > 0).astype(float)
